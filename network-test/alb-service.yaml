apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: alb-ingress-05
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http-healthcheck
  selector:
    app.kubernetes.io/name: alb-demo-app
